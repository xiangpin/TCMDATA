% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PPI_utils.R
\name{getPieData}
\alias{getPieData}
\title{Generate Pie Chart Data for Network Visualization from Enrichment Results}
\usage{
getPieData(
  enrich_obj,
  ppi_genes,
  top_n = 5,
  use_weight = FALSE,
  weight_scale = c("logp", "invp")
)
}
\arguments{
\item{enrich_obj}{An object of class \code{enrichResult}, typically from \code{enrichGO()}, \code{enrichKEGG()}, or \code{enricher()}.}

\item{ppi_genes}{A character vector of gene symbols or IDs present in the PPI network (i.e., \code{V(ppi)$name}).}

\item{top_n}{Integer. The number of top enrichment terms (e.g., GO terms) to include. Default is 5.}

\item{use_weight}{Logical. If \code{TRUE}, uses enrichment significance as weights (e.g., -log10(p.adjust)); otherwise binary (0/1). Default is \code{FALSE}.}

\item{weight_scale}{Character. One of \code{"logp"} (default) or \code{"invp"}. Defines the weighting method if \code{use_weight = TRUE}:
\itemize{
\item \code{"logp"}: use \code{-log10(p.adjust)}
\item \code{"invp"}: use \code{1 / p.adjust}
}}
}
\value{
A data frame with genes in rows and selected enrichment terms in columns. Values represent either binary membership or weighted scores.
}
\description{
This function extracts gene-term relationships from a clusterProfiler enrichment result
and formats them into a matrix suitable for scatterpie-based node pie chart visualization.
}
